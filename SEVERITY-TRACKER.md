# GIGPROOF — Severity Tracker (living)
*Updated: 7 Jul 2026 · Live DB: migration **025** · Owner: Maria · Maintained by Claude Code — updated every build session.*
*Scale: 🔴 blocking now · 🟠 real bug, deferable with a deadline · 🟡 deliberate freeze · 🟢 planned build · ⚪ cleanup debt*

| # | Sev | Item | Blocks | Fix when | Status |
|---|---|---|---|---|---|
| 1 | 🔴 | **Legal P0 (founder):** SEC-01 — Drive folder still not Restricted; Amendment-13 consent spec not yet sent to counsel. ⚠ ESCALATED 8 Jul: founder explicitly opened **public signup** (website `/app` embed) BEFORE the gate — real users + real personal data can now arrive under DRAFT consent texts, so counsel is no longer "before launch", it's "already exposed" | Legal exposure grows with every real signup | **This week, genuinely** | **OPEN — founder action, escalated** |
| 2 | 🟠 | **`ANTHROPIC_API_KEY` is invalid** — `.env.local` holds a Supabase-style token (`sbp_…`), not an Anthropic key (`sk-ant-…`). The automated scan cannot run against the real AI until replaced. | The automated claim-pipeline (LOOP screen "evidence → scan") | **Before wiring the scan screen — do not forget** | OPEN — 1-line fix, needs Maria's real key |
| 3 | 🟡 | **021 vocabulary migration FROZEN** (mirror-only→working-only · booker→booking_manager · consent-scope rename). Written + undo ready (`021_vocabulary_and_consent.sql`), deliberately parked: requires app-code lockstep. | Nothing today (code and DB agree on old words) | At the real-data gate, as one coordinated pass | PARKED by decision (7 Jul) |
| 4 | 🟢 | **LOOP build queue** — ~16 screens on the now-complete schema (consent → onboarding → evidence → claim review → Passport → Radar → publish → action ladder → inbox → pay/ops → gig refresh → share → confirm) | The Gate | Next build sessions | IN PROGRESS — schema done (020–025) |
| 5 | ⚪ | ~~App still writes reactions to legacy `passport_signals`~~ → **DONE for the Passport surface (7 Jul):** the action sheet now writes `professional_reaction` directly (anon+RLS, idempotency key) and page-opens write `passport_view_event`. Remaining: retire the unused `/api/passport-signal` endpoint + legacy `passport_signals` table at cleanup | — | Cleanup pass | MOSTLY CLOSED |
| 6 | ⚪ | **Legacy duplicates:** `profiles`⇄`person` (two identity tables) · `passport_signals`⇄`professional_reaction` · `entitlements`(artist)⇄`subscription`(org) | Nothing — both live, app consistent | Consolidate during LOOP rewiring, table by table | OPEN — debt |
| 7 | ⚪ | **`pr_anon_insert` / `rr_anon_insert` / `ae_any_insert` accept any anonymous row** (spam surface; view-events are already publish-gated, these are not) | Data hygiene at scale | Hardening pass before public launch | OPEN — noted in 024 comments |
| 8 | ⚪ | **`claims.value` is anon-SELECTable** (016 grant; row-gated to published+passport-ok). CLAUDE.md's stricter wording says value should never reach a buyer session — public rendering should move to `public_band`/`public_wording` (now exist, 022) | Firewall depth | When Passport render switches to public_band (LOOP Passport screen) | OPEN — tighten at cutover |
| 9 | ⚪ | **`evidence_artifacts.status` enum is as-built** (`submitted/processed/error`) vs DB-Doc target (`pending/processing/processed/failed/stale`) | Nothing | Fold into 021-class vocabulary pass | PARKED with #3 |
| 10 | ⚪ | **Drive canon copies** still carry pre-correction language (root repo docs fixed 7 Jul; Drive set has its own Cowork update prompt) | Doc consistency for other AIs | Cowork Drive-update run | OPEN — founder/Cowork |
| 11 | ⚪ | **Onboarding artist-create race** — first load can insert TWO artists rows (React dev double-mount; `upsertArtist({created_by})` has no id/conflict key). Caught during live QA of the goal step (2 acts for 1 user). Fix: unique guard or get-or-create RPC | Duplicate rows in dev/QA; unlikely in prod build but real | With the onboarding rewire (LOOP) | OPEN — found 7 Jul |
| 12 | 🟠 | **he.js contains pre-rule machine Hebrew** (e.g. "אושר ע\"י מפיק", "בנד מחיר / גרנטי", gendered "קהילה בבעלותו"). A buyer/artist in HE sees a hybrid: invented Hebrew + English fallback for newer sections. Language direction rule: he.js = native-authored pass by Maria, after features. Decision needed: keep rough HE for now vs. empty it (all-EN until native pass) | Hebrew demo quality (buyer view!) | Maria's call — content is her language domain | OPEN — surfaced in UX audit 7 Jul |
| 13 | 🟠 | **Demo agency roster has no `artist_access` rows** (seed used `created_by` only) → `/agency/radar` feed reads `artist_access` and shows empty. Seed script now creates the linkage (best-effort under RLS); live demo needs ONE approved run: `node scripts/seed-demo-agency.mjs` (or the SQL in the 7 Jul audit report). Also: RadarFeed's `try/finally` has no catch — a query failure renders as "no signals" (add error state at LOOP rewire) | RADAR feed demo | One seed run (founder-approved) | OPEN — found 7 Jul |

## Recently closed
- ✅ **Anonymous Passport read was broken at HEAD (8 Jul):** the 7-Jul leak-guard filtered claims/items by `visibility` — a column the 016 anon grants deliberately exclude — so every TRUE-anonymous buyer got 42501 → "Passport not found" (all recent tests were logged-in). Fixed session-aware: anon relies on the RLS row gate; authenticated viewers get the explicit passport-ok filters. Both classes live-verified (8 Jul)
- ✅ **Public signup shipped via website embed (8 Jul, founder decision):** app served at `/app` on the website project (embed build chain + SPA rewrites + basename + asset merge). E2E verified as true anon: passport render, signup → role select → consent; QA user created and fully erased. Goes live on next git push
- ✅ **Public-view privacy leak** — `getPublicPassport` had NO visibility/status filter (relied on anon RLS alone), so any AUTHENTICATED viewer — the owner, an agency member — saw private/mirror-only claims and items on the "public" Passport. Live-reproduced (Maya's unapproved 180–260 claim rendered on her buyer view), fixed with explicit `passport-ok` + publishable-status filters (defense-in-depth), live-verified gone (7 Jul UX audit)
- ✅ **UX/UI + microcopy audit pass (7 Jul):** `bg-bg` undefined token (transparent app shell + bottom nav) → `bg-paper` · lime-on-light contrast failures (active nav tab, 2 links, ★ chip) → `#657530` · ALL raw-red styling → palette `void`/warm tints (Field/ErrorNote/danger zone/remove/error texts) · 14 "booker(s)" display strings → "booking manager" · "unlocks" gamification wording removed · claim cards now lead with type label + wording (was a naked band) · duplicate ★ Producer-confirmed chip deduped · raw enum `reason_code`/`claim_type` prettified · "OPEN ↗" platform-label bug → SPOTIFY ↗ · Readiness "next step" no longer echoes a status (career-build all-covered line added) · Login dev-jargon removed · "Amendment 13" strings humanized · infinite-spinner risk on AvailabilityRequest fixed · A11 publish hard-block shipped (earlier same day)
- ✅ **018 never applied** (broken policy → full rollback; live DB was silently at 017) → repaired as **019**, applied + verified (7 Jul)
- ✅ **Acts spine missing** (multi-Act canon vs artist-keyed DB) → **020** applied + verified, smoke-tested (7 Jul)
- ✅ **Claim/evidence/gig depth + measurement tables missing** → **022–024** applied + verified (7 Jul)
- ✅ **New fields lacked column-level firewall** → **025** applied + verified: exact_count/contact/working-only fields physically dark to public (7 Jul)
- ✅ **Terminology SSOT (Glossary) locked retired terms** → fixed 7 Jul (Mirror retired; internal scoring scaffold deleted; fill-% entries deleted per standing rule)
