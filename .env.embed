# Loaded by `vite build --mode embed` (the public-website embed of the app).
# These are the PUBLISHABLE client values — they ship inside every browser
# bundle by design (see DEPLOY-APP-PREVIEW.md), so committing them is safe.
# Server secrets (service role, Anthropic) must NEVER go in this file.
VITE_SUPABASE_URL=https://qexfndiyallwqhhzeerd.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_rEoMmflkjGIoAEUFBab_IA_c6k4tgOu
